#!/usr/bin/env node

// This file is the compiled version of src/cli.ts
// It will be generated by the TypeScript compiler

// For now, we'll create a simple wrapper that requires the compiled version
try {
  require('../lib/cli.js');
} catch (error) {
  console.error('Error: Could not load nomad-compose CLI');
  console.error('Please run "npm run build" first to compile the TypeScript code');
  process.exit(1);
}
